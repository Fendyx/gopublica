import { PresentationPage } from '@/widgets/Presentation/PresentationPage';

// This page is intentionally unlisted (no navbar/footer) —
// share the link only during Google Meet calls.
export default function Page() {
  return <PresentationPage />;
}